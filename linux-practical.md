# Linux

hostname -I pour obtenir adresse IP.

Copilot + # pour ouvrir un terminal.

code --ozone-platform=x11 to change the renderer to use for vscode. Ubuntu has two main renderers: wayland and x11. x11 is older and less powerful but wayland can be a little unstable with certain softwares.
